import { makeStyles } from '@material-ui/core/styles';
export const useStyles = makeStyles((theme) => ({
  shopPage: {
    padding: '20px',
    display: 'flex',
    minHeight: '89vh',
    textAlign: 'left',
  },
  Items: {
    display: 'flex',
    flexWrap: 'wrap',
  },
}));
